<%= form_with(model: user_trip) do |form| %>
  <% if user_trip.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_trip.errors.count, "error") %> prohibited this user_trip from being saved:</h2>

      <ul>
        <% user_trip.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :trip_id, style: "display: block" %>
    <%= form.text_field :trip_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
