<%= form_with(model: forbidden_food_user) do |form| %>
  <% if forbidden_food_user.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(forbidden_food_user.errors.count, "error") %> prohibited this forbidden_food_user from being saved:</h2>

      <ul>
        <% forbidden_food_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :ingredient_id, style: "display: block" %>
    <%= form.text_field :ingredient_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
